import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex);


const state = {
	timestamp:null
}

const mutations = {
	
}

const getters = {
}

const actions = {
	
}
export default new Vuex.Store({
	'state':state,
	'mutations':mutations,
	'getters':getters,
	'actions':actions
})
